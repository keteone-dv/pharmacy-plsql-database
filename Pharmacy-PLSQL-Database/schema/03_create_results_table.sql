-- Create table for search results
CREATE TABLE results (
    nome_dci_principio_ativo VARCHAR2(200),
    nome_comercial VARCHAR2(200),
    nome_dosagem VARCHAR2(100),
    nome_embalagem VARCHAR2(150),
    nome_titular_aim VARCHAR2(200),
    preco_embalagem NUMBER(8,2)
);
